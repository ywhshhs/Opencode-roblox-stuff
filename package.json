{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "tsc --build && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --filter \"./packages/cyan-code/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "cyan": "link:packages/cyan-code",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
